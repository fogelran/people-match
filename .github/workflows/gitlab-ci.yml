name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Intall pytest
      run: pip install pytest
    - name: test
      run: pytest
        
